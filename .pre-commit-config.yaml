# run `pre-commit install` once after cloning to activate hooks.
# ruff handles both linting and formatting in a single tool.
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.4
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
